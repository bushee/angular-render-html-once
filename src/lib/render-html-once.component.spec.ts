import {RenderHtmlOnceComponent} from './render-html-once.component';

describe('RenderHtmlOnceComponent', () => {
  let component: RenderHtmlOnceComponent;

  beforeEach(() => {
    component = new RenderHtmlOnceComponent();
  });
});
